#ifndef Buttons_h
#define Buttons_h

class Buttons{
  public:
    Buttons();
    // void Measure();
    bool IsButtonAPressed();  // EM TODO STOP: continue with implementation
    bool IsButtonBPressed();
};

#endif
